#!/usr/bin/env bash

if [[ $EUID -ne 0 ]]; then
  echo "Ejecuta como root: su -c 'yadm bootstrap'"
  exit 1
fi

MY_USER=""

# Parte root (instalar paquetes)
pacman -S --needed neovim git zsh zoxide

# Ejecutar parte de usuario
su - $MY_USER -c "~/.config/yadm/bootstrap_user"
